[Unit]
Description=Timer for MISP IOC Fetcher (Twice a day synchronization)

[Timer]
# Runs at midnight and noon local time
OnCalendar=00/12:00:00
RandomizedDelaySec=300
Persistent=true

[Install]
WantedBy=timers.target
